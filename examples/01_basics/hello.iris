/// The smallest checked IRIS program. Run: iris run examples/01_basics/hello.iris
def main() -> i64 {
    val answer : i64 = 6 * 7;
    assert(answer == 42);
    println(f"Hello, IRIS! answer = {answer}");
    return 0
}
